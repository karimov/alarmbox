

class Alarm(object):
    def __init__(self):
        self.check = False

    def check(self):
        if self.check:
            return True
        return self.check

    def alarm_check(time, device):
        alarms_box = []
        while True:
            Device(ip, data)
            # some logical action should take place in order to verify ports
            if some_thine_wrong:
                alarms_box + some_thing_wrong
            time.sleep(time)
            continue
